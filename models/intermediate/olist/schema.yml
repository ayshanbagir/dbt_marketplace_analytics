version: 2

models:
  - name: int_orders_enriched
    columns:
      - name: order_id
        tests: [not_null, unique]

  - name: int_order_items_enriched
    columns:
      - name: order_id
        tests: [not_null]
      - name: order_item_id
        tests: [not_null]

  - name: int_order_item_facts
    columns:
      - name: order_id
        tests: [not_null]
      - name: order_item_id
        tests: [not_null]
