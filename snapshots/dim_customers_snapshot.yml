snapshots:
  - name: dim_customers_snapshot
    relation: ref('stg_olist_customers')
    config:
      schema: snapshots
      unique_key: customer_id
      strategy: check
      check_cols:
        - customer_unique_id
        - customer_zip_code_prefix
        - customer_city
        - customer_state
      hard_deletes: invalidate