snapshots:
  - name: dim_sellers_snapshot
    relation: ref('stg_olist_sellers')
    config:
      schema: snapshots
      unique_key: seller_id
      strategy: check
      check_cols:
        - seller_zip_code_prefix
        - seller_city
        - seller_state
      hard_deletes: invalidate